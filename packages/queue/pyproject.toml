[project]
name = "queue"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
    "arq>=0.25",
    "redis>=5.0",
    "structlog>=24.0",
    "shared",
]

[tool.uv.sources]
shared = { path = "../shared" }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
